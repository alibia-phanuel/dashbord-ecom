import Layout from "@/Layout";

export default function Chatbot() {
  return (
    <Layout>
      <div
        className="text-3xl flex items-center justify-center w-full "
        style={{ height: "calc(100vh - 32px)" }}
      >
        <h2>Chatbot</h2>
      </div>
    </Layout>
  );
}
