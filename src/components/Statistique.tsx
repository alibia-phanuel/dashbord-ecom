import Layout from "@/Layout";

export default function Statistique() {
  return (
    <Layout>
      <div
        className="text-3xl flex items-center justify-center w-full bg-red-400"
        style={{ height: "calc(100vh - 32px)" }}
      >
        <h2> Statistique</h2>
      </div>
    </Layout>
  );
}
